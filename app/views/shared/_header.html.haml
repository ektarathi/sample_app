%nav.navbar.navbar-default{role: "navigation"}
  .navbar-header
    %button.navbar-toggle{data: { target: "#navbarCollapse", toggle: "collapse"}}
      %span.icon-bar
      %span.icon-bar
      %span.icon-bar
      %span.icon-bar
    = link_to 'Sample App', root_path, class: "navbar-brand"
  #navbarCollapse.collapse.navbar-collapse
    %ul.nav.navbar-nav.navbar-right
      //%li= link_to 'Contact', about_path, class: 'btn btn-default'
      - if logged_in?
        %li= link_to 'Users', users_path, class: 'btn btn-success'
        %li= link_to 'Create Article', new_article_path, class: 'btn btn-default'
        %li.dropdown
          //= link_to 'Account', class: 'btn btn-default dropdown-toggle', data: { toggle: 'dropdown' }
          %a{href: "#", class: "btn btn-default dropdown-toggle", role: "button", data:{ toggle: "dropdown" }}
            Dropdown
            %span.caret
          %ul.dropdown-menu
            %li= link_to 'Profile', current_user
            %li= link_to 'Settings', edit_user_path(current_user)
            %li= link_to 'Logout', logout_path, method: 'delete'
        %li= link_to 'Help', help_path, class: 'btn btn-default'
      - else
        %li= link_to 'Login', login_path, class: 'btn btn-default'
