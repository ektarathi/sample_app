%h2 All Users

= will_paginate

%table.table.table-bordered.table-striped
  %colgroup
    %col.col-sm-6
    %col.col-sm-6
  %tr
    %th{style: "text-align: center"} Name
    %th{style: "text-align: center"} Action
  - @users.each do |user|
    %tr
      %td{style: "text-align: center"}= link_to user.name, user
      %td{style: "text-align: center"}= link_to 'Delete', user, method: :delete, data: { confirm: 'You sure?'}
  - if @users.size == 0
    %tr
      %td.center{colspan: "2"} Sorry: There are currently no users to display.
