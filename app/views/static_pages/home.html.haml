.main-content
  %h1 Welcome to the Sample App.
  %h2
    This is the home page for the
    = link_to 'Ruby on Rails Tutorial', 'http://www.railstutorial.org'
  %h4
    If you would like to signup for latest news and updates, please click on the link below
  %br
  - if !logged_in?
    = link_to 'Create article', new_article_path, class: "btn btn-primary"
  %br
  %br

  %table.table.table-bordered.table-striped
    %colgroup
      %col.col-sm-4
      %col.col-sm-4
      %col.col-sm-4
    %tr
      %th{style: "text-align: center"} HEADING
      %th{style: "text-align: center"} ARTICLE DESCRIPTION
      %th{style: "text-align: center"} ACTION
    - @articles.each do |article|
      %tr
        %td{style: "text-align: center"}= article.title
        %td{style: "text-align: center"}= article.text
        %td{style: "text-align: center"}
          = link_to 'Show', article, class: 'btn btn-warning'
          = link_to 'Edit', edit_article_path(article), class: 'btn btn-primary'
          = link_to 'Delete', article_path(article), method: :delete, data: {confirm: 'Are you sure?'}, class: 'btn btn-danger'
    - if @articles.size == 0
      %tr
        %td.center{colspan: "5"} Sorry: There are currently no articles to display.
